# 通知配置
# 是否启用飞书通知 (true/false)
NOTIFICATION_ENABLED=true

# 是否启用声音提醒 (true/false)
SOUND_ENABLED=true

# 通用 Webhook（飞书 / 钉钉 / 企微，逗号分隔多个）
# 获取方式：
# - 飞书：群设置 -> 群机器人 -> 添加自定义机器人 -> 复制 Webhook
#   示例：https://open.feishu.cn/open-apis/bot/v2/hook/XXXXXXXX
# - 钉钉：群设置 -> 机器人 -> 添加自定义 -> 复制 Webhook
#   示例：https://oapi.dingtalk.com/robot/send?access_token=YYYYYYYY
# - 企微：群聊右上角 ... -> 群机器人 -> 添加 -> 复制 Webhook
#   示例：https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=ZZZZZZZZ
WEBHOOK_URLS=https://open.feishu.cn/open-apis/bot/v2/hook/XXXXXXXX,https://oapi.dingtalk.com/robot/send?access_token=YYYYYYYY
# 是否启用飞书卡片格式（true/false），.env 优先于 settings.json
# WEBHOOK_USE_FEISHU_CARD=false

# Telegram Bot配置
# 获取方法：
# 1. 与 @BotFather 对话创建机器人，获取 token
# 2. 将机器人添加到频道/群组，或直接与机器人对话
# 3. 获取 chat_id (个人ID或频道ID，可通过 https://api.telegram.org/bot<TOKEN>/getUpdates 获取)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# HTTP代理配置（可选）
# 如果需要通过代理访问Telegram API，请配置以下选项之一
# 支持的格式：
# - http://127.0.0.1:7890
# - http://proxy.example.com:8080
# - http://user:password@proxy.example.com:8080
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# 邮件提醒（SMTP）
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=465
# EMAIL_SECURE=true
# EMAIL_USER=bot@example.com
# EMAIL_PASS=your_smtp_password
# EMAIL_FROM=AI提醒 <bot@example.com>
# EMAIL_TO=your@email.com

# AI 摘要（可选）
# SUMMARY_ENABLED=false
# SUMMARY_PROVIDER=openai    # 模型平台：openai | anthropic | google | qwen | deepseek
# SUMMARY_API_URL=https://api.openai.com
# SUMMARY_API_KEY=your_api_key
# SUMMARY_MODEL=gpt-4o-mini
# SUMMARY_TIMEOUT_MS=15000
# SUMMARY_PROMPT=你是一个技术助手，请输出一句简短中文摘要（100字以内）。

# 可选：数据目录/环境文件路径（便于 EXE 版固定存放位置）
# AI_CLI_COMPLETE_NOTIFY_DATA_DIR=C:\Users\YourName\AppData\Roaming\ai-cli-complete-notify
# AI_CLI_COMPLETE_NOTIFY_ENV_PATH=C:\Users\YourName\AppData\Roaming\ai-cli-complete-notify\.env

# Codex completion strategy (optional)
# 1 = only notify when phase=final_answer (default, safer)
# 0 = allow phase-empty assistant messages as completion (legacy compatibility)
# CODEX_STRICT_FINAL_ANSWER=1

# Codex reminder mode (optional)
# 1 = completion-only mode for Codex (default, recommended for gpt-5.3-codex)
# 0 = allow confirm-alert detection in Codex watch output
# CODEX_COMPLETION_ONLY=1

# Fallback quiet window for phase-empty Codex assistant outputs (milliseconds)
# Used only when strict mode is on and no explicit final_answer is emitted.
# CODEX_EMPTY_PHASE_QUIET_MS=120000
